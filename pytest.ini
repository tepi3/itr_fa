[pytest]
testpaths = tests
markers =
    unit: Fast, isolated unit tests (default)
    integration: Tests requiring live server or network access
    ui: Playwright-based UI regression tests
addopts = -v --tb=short -m "not integration and not ui"
